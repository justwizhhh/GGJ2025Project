using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.InputSystem;

public class PlayerController : MonoBehaviour
{
    // Main gameplay logic for player


    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Input functions
    void OnHorizontal(InputValue value)
    {
        //moveSpeed.x = value.Get<Vector2>().x;
    }

    void OnVertical(InputValue value)
    {
        //moveSpeed.y = value.Get<Vector2>().y;
    }

    void OnButton1(InputValue value)
    {

    }

    void OnButton2(InputValue value)
    {

    }

    void OnButton3(InputValue value)
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
